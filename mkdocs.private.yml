# Private handbook-build — draait lokaal of op de beheer-host, NOOIT naar Pages.
# Erft de publieke config; het plugins-blok wordt volledig overschreven (mkdocs
# INHERIT vervangt lijsten) en bevat publiek + privaat. Voor private clones is
# een read-only GH_TOKEN in de omgeving nodig (nooit in dit bestand).
# Bouwen: GH_TOKEN=… uv run mkdocs build -f mkdocs.private.yml -d site-private
INHERIT: mkdocs.yml
site_name: Handbook (privaat)
site_url: ""

plugins:
  - search
  - multirepo:
      cleanup: true
      repos:
        # BEGIN IMPORTS (beheerd door scripts/gen_imports.py)
        - section: estafette
          import_url: 'https://github.com/MWest2020/estafette?branch=main&docs_dir=docs/*'
        - section: habitat
          import_url: 'https://github.com/MWest2020/habitat?branch=main&docs_dir=docs/*'
        - section: homelab
          import_url: 'https://github.com/MWest2020/homelab?branch=main&docs_dir=docs/*'
        - section: Wanderer
          import_url: 'https://github.com/MWest2020/Wanderer?branch=main&docs_dir=docs/*'
        - section: wordsworth
          import_url: 'https://github.com/MWest2020/wordsworth?branch=main&docs_dir=docs/*'
        - section: zeef
          import_url: 'https://github.com/MWest2020/zeef?branch=main&docs_dir=docs/*'
        # END IMPORTS
